---
declared-services:
  conversation-experimental:
    label: conversation
    plan: experimental
applications:
- services:
  - conversation-experimental
  name: personalized-conversation
  command: npm start
  path: .
  instances: 1
  memory: 512MB